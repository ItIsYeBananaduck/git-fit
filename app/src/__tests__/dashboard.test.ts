import { describe, it, expect } from 'vitest';
// import Dashboard from '../routes/dashboard/+page.svelte';

describe('Dashboard UI', () => {
    it('should render purchased programs', () => {
        // TODO: mount Dashboard and check for program list
        expect(true).toBe(true);
    });

    it('should allow CSV download', () => {
        // TODO: simulate click and check download
        expect(true).toBe(true);
    });
});
