// Vitest setup for orb and watch components
import '@testing-library/jest-dom';
import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  // Global setup for orb/watch tests
});
afterAll(() => {
  // Global teardown
});
